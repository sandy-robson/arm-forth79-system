#include <stdio.h> #include <unistd.h> 
char buf[512];
int main(int argc, char *argv[]) 
{ 
  FILE *pFile; 

  pFile = fopen ("/media/mmc1/block.txt", "r"); 
  if (pFile)
  {     printf(" file opened ok\n");
    fgets(buf, 512, pFile);    if (buf != NULL)
    {
      printf(buf);
    }
    fclose (pFile); 
  } 
  return 0; 
}